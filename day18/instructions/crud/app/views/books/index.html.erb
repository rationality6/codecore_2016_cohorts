<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Price</th>
    <th>Length</th>
  </tr>
  <% @books.each do |book|%>
  <tr>
    <td><%= link_to(book.title, book_path(book)) %></td>
    <td><%= book.author %></td>
    <td><%= book.price %></td>
    <td><%= book.length %></td>
  </tr>
  <% end %>

</table>
